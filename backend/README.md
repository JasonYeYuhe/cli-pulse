# Backend Directory

Only `supabase/` is part of the current product path.

Current active contents:

- `supabase/schema.sql`
- `supabase/helper_rpc.sql`
- `supabase/app_rpc.sql`
- migration files under `supabase/`

Archived legacy runtime:

- The older FastAPI backend runtime and its tests have been moved to
  `../archive/backend-fastapi-legacy/`.

If you are looking for the live app/backend contract today, start with
`supabase/`.
