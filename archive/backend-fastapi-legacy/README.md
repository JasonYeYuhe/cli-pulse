# FastAPI Backend Legacy Archive

This archive contains the older backend runtime and tests from the pre-Supabase
path.

Archived here:

- `app/`
- `data/`
- `requirements.txt`
- `requirements-dev.txt`
- `.env.example`
- `__init__.py`
- root `tests/`

These files are not part of the current app-helper-Supabase flow, but are kept
for reference in case older API behavior needs to be consulted.
